groups:
- name: Kafkaalert
  rules:
    - alert: KafkaHighRequestLatency
      expr: kafka_request_latency_ms > 100
      for: 1m 
      labels: 
        severity: warning
      annotations:
        summary: "High request latency kafaka"
        description: "Request latency is above 100 ms for more than 1 minute"